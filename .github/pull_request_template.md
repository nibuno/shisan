## What this changes

## Why

## How it was verified

<!-- The commands you actually ran, and their results. -->

## Checklist

- [ ] `ruff check backend` passes
- [ ] `cd backend && pytest` passes
- [ ] `cd frontend && npm run lint && npm test && npm run build` passes
- [ ] New endpoints have household isolation tests
- [ ] No secrets, real balances, or personal data in the diff
